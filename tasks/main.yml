---
# Variable setup.
- name: Include OS-specific variables.
  include_vars: "{{ ansible_facts.os_family }}.yml"

- name: Define nginx_user.
  set_fact:
    nginx_user: "{{ __nginx_user }}"
  when: nginx_user is not defined

# Setup/install tasks.
- include_tasks: setup-RedHat.yml
  when: ansible_facts.os_family == 'RedHat' or ansible_facts.os_family == 'Rocky' or ansible_facts.os_family == 'AlmaLinux'

- include_tasks: setup-Ubuntu.yml
  when: ansible_facts.distribution == 'Ubuntu'

- include_tasks: setup-Debian.yml
  when: ansible_facts.os_family == 'Debian'

- include_tasks: setup-FreeBSD.yml
  when: ansible_facts.os_family == 'FreeBSD'

- include_tasks: setup-OpenBSD.yml
  when: ansible_facts.os_family == 'OpenBSD'

- include_tasks: setup-Archlinux.yml
  when: ansible_facts.os_family == 'Archlinux'

- include_tasks: setup-Suse.yml
  when: ansible_facts.os_family == 'Suse'

# Cytadel Debian-only: repos nginx.org/blendbyte, migration, sites-*, conf.d.
- include_tasks: migrate.yml
  when:
    - ansible_facts.os_family == 'Debian'
    - nginx_migrate | bool

- include_tasks: modules-include.yml
  when: ansible_facts.os_family == 'Debian'

- include_tasks: confd.yml
  when: ansible_facts.os_family == 'Debian'

- include_tasks: vhosts-Debian.yml
  when: ansible_facts.os_family == 'Debian'

# Upstream vhost layout for every other OS.
- import_tasks: vhosts.yml
  when: ansible_facts.os_family != 'Debian'

# Nginx setup.
- name: Copy nginx configuration in place.
  template:
    src: "{{ nginx_conf_template }}"
    dest: "{{ nginx_conf_file_path }}"
    owner: root
    group: "{{ root_group }}"
    mode: 0644
    #validate: nginx -t -p /etc/nginx/ -c %s
  notify:
    - reload nginx
  when: ansible_facts.os_family != 'Debian' or nginx_manage_main_config | bool

- name: Ensure nginx service is running as configured.
  service:
    name: nginx
    state: "{{ nginx_service_state }}"
    enabled: "{{ nginx_service_enabled }}"
